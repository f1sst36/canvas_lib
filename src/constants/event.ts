export enum UserEvent {
	click = 'click',
	mouseenter = 'mouseenter',
	mouseleave = 'mouseleave',
}

export const UserEvents: UserEvent[] = [UserEvent.click, UserEvent.mouseenter, UserEvent.mouseleave];
